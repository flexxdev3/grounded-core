{
  "name": "@grounded/ui",
  "version": "0.1.0",
  "description": "Grounded console — a small web UI (not a chat app) over the API.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    "./dist/*": "./dist/*"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@grounded/client": "workspace:*",
    "preact": "^10.25.0"
  },
  "devDependencies": {
    "@grounded/core": "workspace:*",
    "@preact/preset-vite": "^2.9.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
