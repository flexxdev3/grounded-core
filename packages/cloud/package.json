{
  "name": "@grounded/cloud",
  "version": "0.1.0",
  "private": true,
  "description": "Grounded Cloud — hosted multi-tenant cabinets (commercial layer, never published).",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "grounded-cloud": "./dist/bin.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@grounded/api": "workspace:*",
    "@grounded/core": "workspace:*",
    "better-auth": "^1.6.0",
    "hono": "^4.12.0",
    "@hono/node-server": "^1.13.7",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.10",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
