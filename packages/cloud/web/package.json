{
  "name": "@grounded/cloud-web",
  "version": "0.1.0",
  "private": true,
  "description": "Grounded Cloud — hosted-cabinet account UI (never published; part of @grounded/cloud).",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@grounded/client": "workspace:*",
    "@grounded/ui": "workspace:*",
    "preact": "^10.25.0"
  },
  "devDependencies": {
    "@grounded/core": "workspace:*",
    "@preact/preset-vite": "^2.9.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
